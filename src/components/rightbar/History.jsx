import { RightbarDetails } from '../../';
import { historyDetails } from '../../utils/constants';

const History = () => {
    return (
        <>
            <RightbarDetails title={null} dataObj={historyDetails} />
        </>
    );
};

export default History;
