import { RightbarDetails } from '../../';
import { prDetails } from '../../utils/constants';

const PRComponent = () => {
    return (
        <>
            <RightbarDetails title={null} dataObj={prDetails} />
        </>
    );
};

export default PRComponent;
