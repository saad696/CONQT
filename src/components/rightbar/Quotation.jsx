import { RightbarDetails } from '../../';
import { quotationDetails } from '../../utils/constants';

const Quotation = () => {
  return (
    <>
        <RightbarDetails title={null} dataObj={quotationDetails} />
    </>
  )
}

export default Quotation